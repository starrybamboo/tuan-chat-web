# 将pnpm变成扁平化架构，pnpm专用
node-linker=hoisted
# 缩短 React Native 原生模块的 CMake/Ninja 路径，避免 Windows 长路径导致 build.ninja 反复重生成
virtual-store-dir=.pnpm
# 设置阿里仓库源
registry="https://registry.npmmirror.com"
# 在国内使用pnpm安装electron需要配置一下electron的下载路径
ELECTRON_MIRROR="https://npmmirror.com/mirrors/electron/"
