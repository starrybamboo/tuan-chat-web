# why

## 目标

- 为“文档卡片消息（DOC_CARD）”的消息右键菜单增加“复制到我的文档 / 复制到空间侧边栏”两个入口。
- 语义为真正复制（copy 一份副本），而不是发送引用。

## 背景与约束

- 聊天中的 DOC_CARD 发送语义为“引用 + 只读预览”，但用户需要从消息中把文档复制为可编辑的新文档。
- 不允许跨 space：复制目标只能落在当前 space。
- “复制到空间侧边栏”仅 KP 可用（侧边栏是空间级资源）。

