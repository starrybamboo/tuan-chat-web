# 技术设计: KP 可选择所有角色发言

## 技术方案
### 核心逻辑
- 在 `ExpressionChooser` 中，当检测到 `isKP` 时，直接使用 `roomRolesThatUserOwn` 作为可选角色列表。
- 非 KP 仍按 `userId` 过滤角色列表，保持原行为。

## 安全与性能
- **安全:** 不涉及权限接口修改，仅调整前端可见性。
- **性能:** 角色列表为已有数据源，无新增请求。

## 测试与验证
- 手动验证 KP 角色面板显示全部角色。
- 验证非 KP 仍仅显示本人角色。
