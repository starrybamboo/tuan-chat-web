diff --git a/dist/core/utils/drag.js b/dist/core/utils/drag.js
index 3ae7416550aedbba36fe707fe498677b53fd8c02..8adb6b4896b6145bc301ab1eed8d64e634eccd44 100644
--- a/dist/core/utils/drag.js
+++ b/dist/core/utils/drag.js
@@ -1,7 +1,16 @@
 import { signal } from '@preact/signals-core';
 export const startDrag = (evt, ops) => {
-    const oldCursor = document.body.style.cursor;
-    document.body.style.cursor = ops.cursor ?? 'grab';
+    const cursorTarget = (() => {
+        const t = evt?.target;
+        const root = t && t.getRootNode?.();
+        const host = root && root.host;
+        if (host instanceof HTMLElement)
+            return host;
+        const closest = t instanceof Element ? t.closest('.blocksuite-portal, .tc-blocksuite-scope') : null;
+        return closest instanceof HTMLElement ? closest : document.body;
+    })();
+    const oldCursor = cursorTarget.style.cursor;
+    cursorTarget.style.cursor = ops.cursor ?? 'grab';
     const mousePosition = signal({
         x: evt.clientX,
         y: evt.clientY,
@@ -20,7 +26,7 @@ export const startDrag = (evt, ops) => {
         window.removeEventListener('pointermove', move);
         window.removeEventListener('pointerup', up);
         window.removeEventListener('keydown', keydown);
-        document.body.style.cursor = oldCursor;
+        cursorTarget.style.cursor = oldCursor;
         ops.onClear();
     };
     const keydown = (evt) => {
diff --git a/src/core/utils/drag.ts b/src/core/utils/drag.ts
index b5443c1aaf2e3aca9f589b7e51965abcd340ecad..36eb27dd75e16a77364c4c0a670f44323dde4e96 100644
--- a/src/core/utils/drag.ts
+++ b/src/core/utils/drag.ts
@@ -16,8 +16,16 @@ export const startDrag = <
     cursor?: string;
   }
 ) => {
-  const oldCursor = document.body.style.cursor;
-  document.body.style.cursor = ops.cursor ?? 'grab';
+  const cursorTarget = (() => {
+    const t = (evt as any)?.target as Element | null | undefined;
+    const root = t?.getRootNode?.() as unknown;
+    const host = (root as any)?.host;
+    if (host instanceof HTMLElement) return host;
+    const closest = t?.closest?.('.blocksuite-portal, .tc-blocksuite-scope');
+    return closest instanceof HTMLElement ? closest : document.body;
+  })();
+  const oldCursor = cursorTarget.style.cursor;
+  cursorTarget.style.cursor = ops.cursor ?? 'grab';
   const mousePosition = signal<{ x: number; y: number }>({
     x: evt.clientX,
     y: evt.clientY,
@@ -36,7 +42,7 @@ export const startDrag = <
     window.removeEventListener('pointermove', move);
     window.removeEventListener('pointerup', up);
     window.removeEventListener('keydown', keydown);
-    document.body.style.cursor = oldCursor;
+    cursorTarget.style.cursor = oldCursor;
     ops.onClear();
   };
   const keydown = (evt: KeyboardEvent) => {
